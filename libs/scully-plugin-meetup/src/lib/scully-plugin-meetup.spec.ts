import { scullyPluginMeetup } from './scully-plugin-meetup';

describe('scullyPluginMeetup', () => {
  it('should work', () => {
    expect(scullyPluginMeetup()).toEqual('scully-plugin-meetup');
  });
});
